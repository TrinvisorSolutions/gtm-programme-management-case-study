fetch('data/programme_data.json').then(r=>r.json()).then(d=>{const labels={milestones_on_track:'Milestones on track',active_customer_engagements:'Active customer engagements',markets:'EMEA markets',high_priority_dependencies:'High-priority dependencies',content_assets_in_flight:'Content assets in flight',stakeholder_actions_overdue:'Overdue stakeholder actions'};document.querySelector('#kpis').innerHTML=Object.entries(d.kpis).map(([k,v])=>`<div class="card"><div class="label">${labels[k]}</div><div class="value">${v}${k==='milestones_on_track'?'%':''}</div></div>`).join('');document.querySelector('#workstreams').innerHTML=d.workstreams.map(w=>`<div class="ws"><strong>${w.name}</strong><div class="label">${w.owner} | ${w.status} | ${w.progress}%</div><div class="bar"><div class="fill" style="width:${w.progress}%"></div></div></div>`).join('');document.querySelector('#raid').innerHTML=`<table><tr><th>ID</th><th>Item</th><th>Impact</th><th>Status</th></tr>${d.risks.map(r=>`<tr><td>${r.id}</td><td>${r.item}</td><td>${r.impact}</td><td>${r.status}</td></tr>`).join('')}</table>`;document.querySelector('#milestones').innerHTML=d.milestones.map(m=>`<p><strong>${m.date}</strong> | ${m.name} | ${m.status}</p>`).join('')});
